---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: assignment-service
  namespace: lms-production
  labels:
    app: assignment-service
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: assignment-service
  template:
    metadata:
      labels:
        app: assignment-service
        tier: backend
    spec:
      containers:
      - name: assignment-service
        image: acrlmsprodlmsrbmfy1.azurecr.io/assignment-service:latest
        ports:
        - containerPort: 8004
          name: http
        env:
        # From ConfigMap
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: lms-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: lms-config
              key: POSTGRES_PORT
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: lms-config
              key: POSTGRES_DB_ASSIGNMENT
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: lms-config
              key: POSTGRES_USER
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: lms-config
              key: ENVIRONMENT
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: lms-config
              key: LOG_LEVEL
        - name: CORS_ORIGINS
          valueFrom:
            configMapKeyRef:
              name: lms-config
              key: CORS_ORIGINS
        # From Secrets
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: lms-secrets
              key: POSTGRES_PASSWORD
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: assignment-service
  namespace: lms-production
  labels:
    app: assignment-service
spec:
  type: ClusterIP
  ports:
  - port: 8004
    targetPort: 8004
    protocol: TCP
    name: http
  selector:
    app: assignment-service
